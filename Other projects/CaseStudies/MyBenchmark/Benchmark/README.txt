Main Class:

Tester ... it takes a number (0-4) as a command line argument which represents the test number. The first test does not violate any of the properties. Then each of the other properties violate a particular property. 


Other Classes:

Bank   ... contains a number of users, executes transactions

User   ... contains a number of transactions

Transaction ... can be executed

DummyDatabase ... represents a database, has some attributes which are directly manipulated by the Tester class to change the way transaction succeed or fail.
